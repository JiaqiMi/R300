obstacle_range: 4.0
raytrace_range: 5.0

# 2026-07-30 操作员决定改回实车尺寸 0.80×0.70(±0.40×±0.35)。
# 历史: 2026-07-29 曾定 ±0.36(比实车短8cm换通过性); 快反层+侵蚀修复后感知
# 距离已到 7~9m, 不再需要用"虚报车长"换通过性, 回归真实几何。
# 联动核对: 内切半径0.35不变(膨胀0.40/1.3合法); 外接圆0.502→0.532
# (min_range 0.60 仍罩住自车格); speed_limit_margin 1.2 的分解本按保险杠
# 0.40 推导, 改后精确对齐; 脱困车尾走廊 -0.42 起点在车尾 -0.40 之后仍成立。
footprint: [[0.40, 0.35],
            [0.40, -0.35],
            [-0.40, -0.35],
            [-0.40, 0.35]]


map_type: costmap

observation_sources: scan

scan:
  sensor_frame: livox_frame
  data_type: LaserScan
  topic: /scan
  marking: true
  clearing: true
  observation_persistence: 0.0
  expected_update_rate: 0.0
  inf_is_valid: true
