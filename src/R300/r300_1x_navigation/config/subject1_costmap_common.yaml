obstacle_range: 4.0
raytrace_range: 5.0

# 2026-07-29 实车实测 0.80x0.70m: footprint=真实尺寸(半长0.40/半宽0.35)+padding 0.02。
# 原0.45虚大10cm(过保守); 现场曾改0.36比实车小8cm(会蹭障碍, 已纠正)——
# 想更敢走请调膨胀, footprint 永远等于真车。
# 2026-07-29 操作员决策: footprint 取 ±0.36(比实车0.80长度小8cm), 用两端各4cm的
# 蹭障风险换窄道通过性——知情权衡, 勿当bug改回。实车真实尺寸 0.80x0.70。
footprint: [[0.36, 0.35],
            [0.36, -0.35],
            [-0.36, -0.35],
            [-0.36, 0.35]]


map_type: costmap

observation_sources: scan

scan:
  sensor_frame: livox_frame
  data_type: LaserScan
  topic: /scan
  marking: true
  clearing: true
  observation_persistence: 0.0
  expected_update_rate: 0.0
  inf_is_valid: true
