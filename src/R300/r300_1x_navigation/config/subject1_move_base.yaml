recovery_behavior_enabled: false
shutdown_costmaps: false

controller_frequency: 5.0
planner_frequency: 1.0   # 2026-07-29 0→1.0: 障碍堵住直线后全局路径自动重算, 根治"DWA failed to produce path"卡死(recovery已禁用无自救)

planner_patience: 8.0
controller_patience: 8.0

conservative_reset_dist: 3.0

# 纯 DWA 验证阶段关闭 move_base 的振荡超时干预。
oscillation_timeout: 0.0
oscillation_distance: 0.10 

latch_xy_goal_tolerance: true