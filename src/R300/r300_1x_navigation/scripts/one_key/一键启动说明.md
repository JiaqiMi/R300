# R300 一键启动说明

脚本目录：

```bash
cd ~/r300_ws/src/R300/r300_1x_navigation/scripts/one_key
```

---

## 1. 实车导航

脚本：`start_real_nav.sh`

功能：启动 1X 惯导、`map→odom`、`move_base + DWA`、航点执行器和底盘节点。DWA 通过 `/subject1/cmd_vel_raw` 直接控制底盘。

### 常用命令

```bash
./start_real_nav.sh
```

指定惯导串口：

```bash
INS_PORT=/dev/ttyUSB0 ./start_real_nav.sh
```

启动 RViz：

```bash
LAUNCH_RVIZ=true ./start_real_nav.sh
```

启动后自动执行航点：

```bash
AUTO_START=true ./start_real_nav.sh
```

配置 CAN 后启动：

```bash
SETUP_CAN=true CAN_PORT=can0 ./start_real_nav.sh
```

手动开始航点：

```bash
rosservice call /subject1/start_waypoints "{}"
```

---

## 2. DWA 仿真

脚本：`start_sim_dwa.sh`

功能：启动空白地图、虚拟 R300、`move_base + DWA`、仿真 `/one_x/odom` 和轨迹显示。

### 常用命令

```bash
./start_sim_dwa.sh
```

不启动 RViz：

```bash
RVIZ=false ./start_sim_dwa.sh
```

加入位置漂移和航向噪声：

```bash
DRIFT_Y_MPS=0.03 YAW_NOISE_DEG=0.5 ./start_sim_dwa.sh
```

加入周期性位置跳变：

```bash
JUMP_PERIOD_S=3.0 JUMP_STD_M=0.2 ./start_sim_dwa.sh
```

---

## 3. 仅启动惯导定位

脚本：`start_localization_only.sh`

功能：仅启动 `one_x_serial_driver`，发布 `/one_x/odom`、定位、航向和 TF，不启动导航与底盘。

### 常用命令

```bash
./start_localization_only.sh
```

指定串口：

```bash
INS_PORT=/dev/ttyUSB0 ./start_localization_only.sh
```

适用于惯导对准、里程计稳定性和 INS/GPS 数据检查。

---

## 4. 发送测试目标

脚本：`send_goal_base.sh`

目标坐标系为 `base_link`，适合直接测试 DWA。

```bash
./send_goal_base.sh forward 10
```

```bash
./send_goal_base.sh back 5
```

```bash
./send_goal_base.sh left 3
```

```bash
./send_goal_base.sh right 3
```

自定义坐标：

```bash
./send_goal_base.sh xy 10 -2
```

---

## 5. 录制 ROS Bag

脚本：`record_r300_bag.sh`

```bash
./record_r300_bag.sh
```

默认保存位置：

```text
~/bags/r300_年月日_时分秒.bag
```

自定义文件名：

```bash
OUT=~/bags/dwa_test_01 ./record_r300_bag.sh
```

主要记录：

```text
/subject1/cmd_vel_raw
/one_x/odom
/one_x/path
/one_x/fix
/one_x/ins_fix
/one_x/gps_fix
/one_x/heading_deg
/one_x/pos_compare
/move_base/NavfnROS/plan
/move_base/DWAPlannerROS/local_plan
/move_base/current_goal
/tf
/tf_static
```

---

## 6. 检查导航链路

脚本：`check_r300_nav.sh`

```bash
./check_r300_nav.sh
```

检查内容：

- ROS 节点状态；
- `/subject1/cmd_vel_raw` 发布与订阅；
- `/one_x/odom` 发布与订阅；
- `/map`；
- `move_base` 关键参数；
- `odom→base_link` TF。

实车正常链路：

```text
/subject1/cmd_vel_raw：/move_base → /scout_base_node
/one_x/odom：/one_x_serial_driver → /move_base
```

仿真正常链路：

```text
/subject1/cmd_vel_raw：/move_base → /sim_r300_odom_node
/one_x/odom：/sim_r300_odom_node → /move_base
```

---

## 7. 停止导航系统

脚本：`stop_r300_nav.sh`

```bash
./stop_r300_nav.sh
```

用于停止导航、惯导、底盘、仿真、地图、轨迹和 RViz 等相关节点。

---

## 8. 修复脚本权限

脚本：`fix_permissions.sh`

```bash
./fix_permissions.sh
```

用于解决：

```text
Permission denied
cannot launch node of type ...
```

---

## 9. 户外视觉避障导航

启动视觉导航：

```bash
./start_r300_vision_nav.sh --no-rviz
```

启动 Web costmap 查看器：

```bash
python3 ~/r300_ws/src/R300/r300_1x_navigation/scripts/costmap_web_viewer.py _port:=8088
```

减少小碎片障碍标注：

```bash
python3 ~/r300_ws/src/R300/r300_1x_navigation/scripts/costmap_web_viewer.py _port:=8088 _min_obstacle_cluster_beams:=4 _max_obstacle_labels:=5
```

查看工控机 IP：

```bash
hostname -I
```

浏览器访问：

```text
http://工控机IP:8088
```

示例：

```text
http://192.168.1.107:8088
```

运行视觉 costmap 诊断：

```bash
python3 ~/r300_ws/src/R300/r300_1x_navigation/scripts/diagnose_vision_costmap.py _duration_s:=20
```

---

# 推荐测试流程

## 仿真

终端 1：

```bash
./start_sim_dwa.sh
```

终端 2：

```bash
./send_goal_base.sh forward 10
```

终端 3：

```bash
./record_r300_bag.sh
```

## 实车

终端 1：

```bash
INS_PORT=/dev/ttyACM0 CAN_PORT=can0 ./start_real_nav.sh
```

终端 2：

```bash
./check_r300_nav.sh
```

链路正常后启动航点：

```bash
rosservice call /subject1/start_waypoints "{}"
```

## 视觉避障

终端 1：

```bash
./start_r300_vision_nav.sh --no-rviz
```

终端 2：

```bash
python3 ~/r300_ws/src/R300/r300_1x_navigation/scripts/costmap_web_viewer.py _port:=8088
```

终端 3：

```bash
./send_goal_base.sh forward 5
```
