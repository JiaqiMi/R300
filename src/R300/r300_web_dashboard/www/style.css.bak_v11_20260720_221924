:root {
  --bg: #041006;
  --panel: #0c2a11;
  --panel2: #061707;
  --cardTop: #123919;
  --text: #f3ffe9;
  --muted: #b6d4a3;
  --line: rgba(151, 219, 72, .28);
  --accent: #9bea22;
  --accent2: #d7f549;
  --good: #91e928;
  --bad: #ef4444;
  --warn: #f5d32f;
}
* { box-sizing: border-box; }
html, body { min-height: 100%; }
body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei", Arial, sans-serif;
  background:
    radial-gradient(circle at 18% 0%, rgba(151,234,34,.22), transparent 24rem),
    radial-gradient(circle at 90% 10%, rgba(214,245,73,.14), transparent 20rem),
    var(--bg);
  color: var(--text);
}
.topbar {
  position: sticky; top: 0; z-index: 5;
  display: flex; align-items: center; justify-content: space-between; gap: 14px;
  padding: 14px 18px;
  background: rgba(3, 16, 5, .94);
  border-bottom: 1px solid var(--line);
  backdrop-filter: blur(8px);
}
.brand { display:flex; align-items:center; gap:14px; }
.brand-mark { width:44px; height:44px; border-radius:12px; display:grid; place-items:center; font-size:28px; font-weight:900; color:#08240b; background:linear-gradient(135deg,#e2ff61,#78d51d); box-shadow:0 0 28px rgba(151,234,34,.28); }
h1 { margin: 0; font-size: 27px; letter-spacing: .5px; }
h1 span { color:#ecffcf; font-weight:900; }
h1 em { color:#b8df65; margin:0 12px; font-style:normal; opacity:.7; }
p { margin: 4px 0 0; color: var(--muted); }
.status-bar { display: flex; gap: 8px; flex-wrap: wrap; justify-content: flex-end; }
.badge { display: inline-block; padding: 6px 10px; border-radius: 999px; background: rgba(94,130,42,.32); color: var(--text); font-size: 13px; white-space: nowrap; }
.badge.good { background: rgba(145,233,40,.20); color: #c8ff7b; border: 1px solid rgba(145,233,40,.36); }
.badge.bad { background: rgba(239,68,68,.18); color: #fecaca; border: 1px solid rgba(239,68,68,.28); }
.dashboard {
  display: grid;
  grid-template-columns: minmax(430px, 1fr) minmax(430px, 1fr) minmax(430px, 1fr);
  gap: 14px;
  padding: 14px;
  align-items: stretch;
}
.card {
  background: linear-gradient(180deg, rgba(16,49,19,.96), rgba(5,22,7,.97));
  border: 1px solid var(--line);
  border-radius: 16px;
  padding: 13px;
  overflow: hidden;
  box-shadow: 0 14px 32px rgba(0,0,0,.28), inset 0 1px 0 rgba(214,245,73,.08);
}
.hero-card { min-height: 635px; }
.card-title { display: flex; align-items: center; justify-content: space-between; gap: 10px; margin-bottom: 10px; }
h2 { margin: 0; font-size: 18px; }
.card-title span { color: var(--muted); font-size: 12px; }
.video-wrap {
  width: 100%; height: 500px;
  display: flex; align-items: center; justify-content: center;
  background: #020617; border: 1px solid rgba(151,219,72,.20); border-radius: 10px;
}
.video-card img { width: 100%; height: 100%; object-fit: contain; background: #000; border-radius: 10px; }
canvas { width: 100%; height: 500px; background: #020617; border-radius: 10px; border: 1px solid rgba(151,219,72,.20); display: block; }
.toolbar { display: flex; flex-wrap: wrap; gap: 9px 12px; margin-top: 10px; color: var(--muted); font-size: 12px; align-items: center; }
.toolbar button { padding: 5px 9px; font-size: 12px; border-radius: 7px; }
.status-card { grid-row: auto; }
.scan-card, .detection-card { min-height: 360px; }
.control-card { min-height: 520px; }
.kv { display: flex; justify-content: space-between; gap: 14px; padding: 8px 0; border-bottom: 1px solid rgba(148,163,184,.13); }
.kv span { color: var(--muted); }
.kv b { font-family: Consolas, "JetBrains Mono", monospace; text-align: right; font-size: 13px; }
.mini-title { color: #facc15; font-weight: 700; margin: 14px 0 4px; }
.buttons { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 9px; }
button { cursor: pointer; background: linear-gradient(180deg, #a8f02a, #5fad13); color: white; border: 0; border-radius: 9px; padding: 10px 12px; font-weight: 700; }
button:hover { filter: brightness(1.12); }
button.danger { background: linear-gradient(180deg, #f87171, #dc2626); grid-column: span 2; }
button.soft { background: linear-gradient(180deg, rgba(139,181,70,.92), rgba(67,103,37,.92)); }
button.kiwi { background: linear-gradient(180deg, #d7f549, #75c51c); color:#08240b; box-shadow:0 0 18px rgba(151,234,34,.22); }
pre { background: var(--panel2); border: 1px solid rgba(151,219,72,.20); color: #d1d5db; border-radius: 10px; padding: 10px; overflow: auto; max-height: 360px; font-size: 12px; line-height: 1.45; white-space: pre-wrap; }
.legend { display: flex; flex-wrap: wrap; gap: 9px; margin-top: 12px; color: var(--muted); font-size: 12px; }
.legend i { display: inline-block; width: 12px; height: 12px; border-radius: 3px; margin-right: 4px; vertical-align: -2px; }
.robot { background: #3b82f6; clip-path: polygon(50% 0, 100% 100%, 50% 70%, 0 100%); }
.black { background: #040404; border: 1px solid #fff; }
.gray { background: #7a7a7a; }
.green { background: #22c55e; }
.cyan { background: #38bdf8; }
.red { background: #ef4444; }
.orange { background: #f97316; }
.purple { background: #a855f7; }
footer { display: flex; justify-content: space-between; gap: 10px; padding: 10px 16px; color: var(--muted); border-top: 1px solid var(--line); font-size: 12px; }
@media (max-width: 1500px) { .dashboard { grid-template-columns: 1fr 1fr; } .status-card { grid-row: auto; } }
@media (max-width: 1000px) { .dashboard { grid-template-columns: 1fr; } .hero-card { min-height: auto; } .video-wrap, canvas { height: 58vw; min-height: 360px; } header, footer { flex-direction: column; align-items: flex-start; } }

.node-actions { display:flex; align-items:center; gap:10px; margin-top:10px; flex-wrap:wrap; }
.node-actions button { min-width:130px; padding:8px 12px; }
.map-node-actions { margin-top:10px; margin-bottom:2px; }
.proc-state { color:#cef995; background:rgba(145,233,40,.10); border:1px solid rgba(145,233,40,.22); padding:6px 10px; border-radius:999px; font-size:12px; }
#serviceLog { height: 210px; max-height: 210px; margin-top: 12px; }
#nodeLog { height: 360px; max-height: 360px; margin-top:10px; }


/* =========================================================
 * v8 一行主视图布局
 * 第一行：摄像头 / local_costmap + DWA / 卫星轨迹
 * 第二行：雷达俯视图 / 车辆状态 / 航点与日志
 * 第三行：视觉检测信息
 * ========================================================= */
.video-card { grid-column: 1; grid-row: 1; }
.map-card { grid-column: 2; grid-row: 1; }
.satellite-card { grid-column: 3; grid-row: 1; min-height: 635px; }
.scan-card { grid-column: 1; grid-row: 2; min-height: 520px; }
.status-card { grid-column: 2; grid-row: 2; min-height: 520px; }
.control-card { grid-column: 3; grid-row: 2; min-height: 520px; }
.detection-card { grid-column: 1 / -1; grid-row: 3; min-height: 320px; }

/* 卫星图放在第一行第三列，因此内部改成上下布局，避免横向太挤。 */
.satellite-layout {
  display: grid;
  grid-template-columns: 1fr;
  grid-template-rows: auto auto;
  gap: 10px;
  align-items: stretch;
}
#satelliteMap {
  width: 100%;
  height: 500px;
  min-height: 500px;
  border-radius: 12px;
  overflow: hidden;
  border: 1px solid rgba(151,219,72,.24);
  background: #061707;
  box-shadow: inset 0 0 0 1px rgba(214,245,73,.05);
}
.satellite-side {
  background: rgba(5,22,7,.62);
  border: 1px solid rgba(151,219,72,.18);
  border-radius: 12px;
  padding: 10px;
  max-height: 140px;
  overflow: auto;
}
.satellite-side .mini-title { margin-top: 0; }
.satellite-side .kv { padding: 4px 0; }
.map-buttons {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 7px;
  margin-top: 8px;
}
.map-buttons button { padding: 7px 6px; font-size: 12px; }
.map-note { display: none; }
.leaflet-container { background:#061707; font-family:-apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei", Arial, sans-serif; }
.leaflet-control-zoom a { background:#102f15 !important; color:#d7f549 !important; border-color:rgba(151,219,72,.28) !important; }
.leaflet-control-attribution { background:rgba(3,16,5,.70) !important; color:#b6d4a3 !important; }
.vehicle-marker { width:0; height:0; border-left:10px solid transparent; border-right:10px solid transparent; border-bottom:28px solid #a8ff34; filter: drop-shadow(0 0 8px rgba(168,255,52,.8)); transform-origin:50% 70%; }
.start-marker, .current-marker { font-weight:800; color:#08240b; background:#d7f549; border:2px solid #102f15; border-radius:999px; width:28px; height:28px; display:grid; place-items:center; box-shadow:0 0 12px rgba(168,255,52,.55); }

/* 日志继续放大，便于第二行观察。 */
#serviceLog { height: 150px; max-height: 150px; margin-top: 12px; }
#nodeLog { height: 270px; max-height: 270px; margin-top: 10px; }

@media (max-width: 1600px) {
  .dashboard { grid-template-columns: 1fr 1fr; }
  .video-card { grid-column: 1; grid-row: 1; }
  .map-card { grid-column: 2; grid-row: 1; }
  .satellite-card { grid-column: 1 / -1; grid-row: 2; min-height: 600px; }
  .scan-card { grid-column: 1; grid-row: 3; }
  .status-card { grid-column: 2; grid-row: 3; }
  .control-card { grid-column: 1 / -1; grid-row: 4; }
  .detection-card { grid-column: 1 / -1; grid-row: 5; }
  #satelliteMap { height: 480px; min-height: 480px; }
  .satellite-side { max-height: 165px; }
}
@media (max-width: 1100px) {
  .dashboard { grid-template-columns: 1fr; }
  .video-card, .map-card, .satellite-card, .scan-card, .status-card, .control-card, .detection-card { grid-column: 1; grid-row: auto; }
  .hero-card { min-height: auto; }
  .video-wrap, canvas { height: 58vw; min-height: 360px; }
  #satelliteMap { height: 520px; min-height: 520px; }
  .satellite-side { max-height: none; }
  .map-buttons { grid-template-columns: 1fr 1fr; }
  header, footer { flex-direction: column; align-items: flex-start; }
}

/* =========================================================
 * v9 两行总览布局
 * 第一行：摄像头 / local_costmap + DWA / 卫星轨迹
 * 第二行：雷达俯视图 / 车辆状态 / 航点控制日志 / 视觉检测日志
 * ========================================================= */
.dashboard {
  grid-template-columns: minmax(360px, 1fr) minmax(360px, 1fr) minmax(360px, 1fr) minmax(360px, 1fr);
  gap: 14px;
}
.video-card { grid-column: 1; grid-row: 1; min-height: 635px; }
.map-card { grid-column: 2; grid-row: 1; min-height: 635px; }
.satellite-card { grid-column: 3 / 5; grid-row: 1; min-height: 635px; }
.scan-card { grid-column: 1; grid-row: 2; min-height: 520px; }
.status-card { grid-column: 2; grid-row: 2; min-height: 520px; }
.control-card { grid-column: 3; grid-row: 2; min-height: 520px; }
.detection-card { grid-column: 4; grid-row: 2; min-height: 520px; }

.video-wrap, canvas {
  height: 500px;
}
#satelliteMap {
  height: 500px;
  min-height: 500px;
}

/* 第二行四个卡片变窄后，控制区和日志区做紧凑化。 */
.buttons {
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 8px;
}
.buttons button {
  padding: 8px 8px;
  font-size: 13px;
}
#serviceLog {
  height: 130px;
  max-height: 130px;
  margin-top: 10px;
}
#nodeLog {
  height: 250px;
  max-height: 250px;
  margin-top: 10px;
}

/* 视觉检测信息放到第二行后，增加日志高度，减少空白。 */
#detections {
  height: 250px;
  max-height: 250px;
}
#targetPoint {
  height: 150px;
  max-height: 150px;
}
.detection-card pre {
  margin: 10px 0 0;
}

/* 卫星地图第一行占两列，右侧信息栏重新横向排列，避免遮挡地图高度。 */
.satellite-layout {
  display: grid;
  grid-template-columns: minmax(520px, 1fr) 300px;
  grid-template-rows: auto;
  gap: 12px;
  align-items: stretch;
}
.satellite-side {
  max-height: 500px;
  overflow: auto;
}
.map-buttons {
  grid-template-columns: 1fr 1fr;
}
.map-note {
  display: block;
  font-size: 11px;
  line-height: 1.45;
}

/* 二行布局需要更宽屏；中等屏幕自动退回三行，避免卡片太窄。 */
@media (max-width: 1800px) {
  .dashboard {
    grid-template-columns: 1fr 1fr 1fr;
  }
  .video-card { grid-column: 1; grid-row: 1; }
  .map-card { grid-column: 2; grid-row: 1; }
  .satellite-card { grid-column: 3; grid-row: 1; }
  .scan-card { grid-column: 1; grid-row: 2; }
  .status-card { grid-column: 2; grid-row: 2; }
  .control-card { grid-column: 3; grid-row: 2; }
  .detection-card { grid-column: 1 / -1; grid-row: 3; }
  .satellite-layout { grid-template-columns: 1fr; }
  .satellite-side { max-height: 140px; }
  .map-buttons { grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .map-note { display: none; }
  #detections { height: 220px; max-height: 220px; }
  #targetPoint { height: 90px; max-height: 90px; }
}

@media (max-width: 1200px) {
  .dashboard { grid-template-columns: 1fr; }
  .video-card, .map-card, .satellite-card, .scan-card, .status-card, .control-card, .detection-card {
    grid-column: 1;
    grid-row: auto;
  }
  .hero-card { min-height: auto; }
  .video-wrap, canvas { height: 58vw; min-height: 360px; }
  #satelliteMap { height: 520px; min-height: 520px; }
  .satellite-layout { grid-template-columns: 1fr; }
  .satellite-side { max-height: none; }
  .map-buttons { grid-template-columns: 1fr 1fr; }
  #serviceLog, #nodeLog, #detections, #targetPoint { height: auto; max-height: 360px; }
}
